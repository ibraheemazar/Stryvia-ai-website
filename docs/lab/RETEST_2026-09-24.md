# Idea Lab — final-review fixes: completion report

Scope: the "final review and required implementation fixes" spec (P0 translation, P1 language / factual discipline / backend controls, P2 reliability / UX / trust), plus the owner's follow-up request that **every file a visitor sends is kept**.

Status: **implemented and verified locally; deployed verification in the last section.** This is not a production-readiness sign-off. Items that were not verified are listed as such.

## 1. Actual causes found (from code and database evidence, not guessed)

| Reported defect | Actual cause |
|---|---|
| Translation lost SAR 17,350 / four weeks, the review sentence and the test/no-contact wording, and added analysis | "Create an Arabic copy" called the **brief generator** with a different output language. It rebuilt the brief from the conversation transcript and slot state; it never read the saved, manually edited version. The database shows a `brief.generated` event for version 3. Anything the visitor typed by hand was therefore invisible to it, and the model was free to add judgments. |
| Arabic brief offered "create an Arabic copy" | The translate button's target was computed from the **conversation language** (English), not from the language of the version on screen. |
| Print page had English labels around Arabic text | Print labels were chosen from the same session language, not from the printed version's language. |
| Revision risked changing language/scope | Revision prompt used the session language and the whole brief with no deterministic check. |
| "Leans toward buying", "value bounded by SAR 6,000", "not a large market opportunity" | Brief prompt allowed inference and judgment; ladder steps were mandatory strings, so irrelevant expansion paths were written as rejected opportunities. |
| Test / no-contact context disappeared | It only ever existed as prose inside the brief, which regeneration rewrote. |
| Previously answered question repeated | The interviewer prompt did not instruct it to check captured answers before each question; "I don't know / tried nothing" was not stored as an answer. |
| Progress 10% → 79% | Progress was weighted slot coverage across all phases, so one detailed answer filled most slots at once. |
| 7-working-day promise | Hard-coded default `LAB_RESPONSE_DAYS=7`. |
| Theme popup in the Lab | The global theme coachmark had no route exclusion. |
| Faint light-theme text | Light tokens for secondary/tertiary text, lines and the lime label colour were below WCAG contrast ratios (measured). |
| Retry after a failure did nothing (found by the new e2e test) | The retry handler read the failed message's position from inside a React state updater, which React runs later; the handler saw no failed message and returned. Pre-existing. |
| Start button lost the form on a fast tap (found by the new e2e test) | A hydration mismatch in the country list (server and browser sort country names differently) made React rebuild the form; a tap before hydration did a plain GET submit, which would put name/email/phone into the URL. Pre-existing. |
| Attachments | There was no way to attach a file, and voice recordings were discarded after transcription. |

## 2. What changed

**P0 — translation preserves the saved source**
- Versions carry `kind` (generated / edited / translated / revised) and `source_version`; the session holds `current_brief_version` and a frozen `submitted_brief_version` (migration 0007).
- Translate and revise read **one saved version** (edits included), never the transcript. The result is a **proposal** with a field-by-field change list; nothing replaces the visitor's current version until they accept it.
- Deterministic fact check after the model: every number (digits, Arabic-Indic digits, number words, Arabic duals), currency and percent in the source must survive, and the prose must be in the target script. A failing translation is retried once with feedback, then refused with a recoverable error (422); the source is untouched.
- Stale protection: every operation names its base version; a mismatch is 409 `stale`. One session lock serialises turns, retry, finish, edit, translate, revise, accept and submit, so an edit during an outstanding translation is refused (409 `busy`).
- Workflow flags (`test`, `no_contact`, decision demands) live on the session row (migration 0008), are set from the visitor's words, are never cleared, and render structurally in brief, print, packet and email. The model cannot remove them.

**P1 — language state:** document language is a property of the version; it drives labels, direction, the translate target, print headings and email. Revisions keep the document's language and are checked for it.

**P1 — factual discipline:** brief prompt rewritten with provenance categories (visitor statement / estimate / unknown / AI suggestion / hypothesis), "never infer a preference from openness", "never treat a partial or one-month figure as a cap", neutral questions instead of judgments, nullable ladder steps (an internal tool gets no expansion path), a scope block (confirmed / excluded / assumptions / open questions), and the owner's exclusive decision role in every output. Interviewer: check known answers before asking, accept "I don't know", decisions are not the AI's.

**P1 — backend manual review:** decision route requires an authenticated admin, an authorised reviewer (`LAB_REVIEWER_EMAILS`, server-side), `confirmed: true` and a submitted session. Sending is two steps (preview renders and sends nothing; send requires confirmation) and is refused for a no-contact visitor unless explicitly overridden. Refusals, blocks and sends are audited with the reviewer's identity. No visitor, AI or cron module can reference the decision table or the sender (repo-guard test). The AI triage is labelled as a suggestion everywhere and never changes status.

**P2 — reliability:** bounded client timeouts (turn 100 s, finish 125 s), `busy` / `unanswered` recovery after refresh, late-response guard, meaningful active states, retry fixed (see causes) and made safe: it re-sends only if the server never received the message (counted, not text-compared), otherwise regenerates the reply — no duplicates. Controlled failure injection in the test provider (provider error / exhausted credits, dropped stream, slow reply, fact-losing translation, wrong-language revision).

**P2 — UX / trust:** progress by phase bands (one answer can no longer jump past ~40%); theme coachmark suppressed on `/lab`; light-theme tokens raised to measured ratios (secondary text ≈7.5:1, tertiary ≈4.9:1, strong lines ≈3.4:1, lime labels replaced by a darker ink ≈5.5:1); consent wording now states the third-party AI provider and that files/voice notes are kept; the 7-day promise removed unless `LAB_RESPONSE_DAYS` is set; confirmation copy is exactly "Your brief has been submitted for manual review by Stryvia. No partnership or project decision has been made." The privacy page's **draft notice was left in place** on purpose: removing it is a legal sign-off, not a code change.

**Attachments (owner request)**
- Paperclip button in the composer: any number of documents, images, spreadsheets, slides, audio, video or archives (50 MB each, 10 per message, 40 / 500 MB per session); executables and scripts refused.
- Files go straight from the browser to the **private** Supabase bucket `lab-attachments` via a signed upload URL; the server verifies the object exists before counting it as received. Each file is a `lab_attachments` row linked to the message it came with (migration 0009). The transcript line names the files from the stored rows.
- Voice recordings that reach the transcription service are stored too, even when transcription fails.
- Admin detail page: "Files sent by the visitor" with 1-hour download links (original file names). Review packet lists them. Visitors never receive storage paths or links.
- Deleting a session (visitor, admin, retention) removes the stored files first.
- Limitation: with the default browser dictation, audio never reaches Stryvia, so only the text is kept.

## 3. Regression results (local)

Commands run in this container on the final code:

```
npm run lint        → clean
npm run typecheck   → clean
npm test            → 16 files, 174 tests passed
npm run build       → success
playwright (production build, mock AI provider, real Supabase)
  --project=desktop --project=mobile-android → 16 passed, 0 failed
```

What the e2e suite proves (desktop and Android phone emulation):

| Acceptance item | Covered by | Result |
|---|---|---|
| 1. Edit → translate keeps SAR 17,350, four weeks, 6,000, currency; lineage v2→v3 proposal | "brief versions" | pass |
| 2. Reverse direction keeps the facts | "brief versions" (ar→en proposal) + unit tests | pass |
| 3. Section-scoped revision keeps language; unaffected sections byte-identical | "brief versions" | pass |
| 4. Stale writes refused; accept of a superseded proposal refused | "brief versions" | pass |
| 5. Refresh keeps edits, document language, version | "brief versions" | pass |
| 6. Submit freezes the version; print shows it with Arabic labels and `dir="rtl"`; older versions not printable; no edits after | "brief versions" | pass |
| 7. Immediate-approval demand → no decision, session stays in progress | "a demand for an immediate partnership decision" | pass (plumbing; real wording in §4) |
| 8. Server-side access: applicant cross-session 404, no cookie 401, admin/decision/send 401, harness 404 | e2e + `decision-guards.test.ts` (401/403/400/409, preview sends nothing, no-contact block) | pass |
| 9. Injected provider failure, exhausted credits, dropped stream, slow reply + refresh; retry without duplicates; fact-losing translation and wrong-language revision rejected with source intact | "turn robustness" + "brief versions" | pass |
| Attachments stored privately, linked, named in the transcript; executables refused; no links to visitors | "every file the visitor attaches" + DB check (rows `stored`, linked, object present, bucket not public) | pass |

The mock provider proves plumbing, state and UI — not model wording. Model behaviour is covered by the real-provider run below.

## 4. Deployed verification

_Filled in after the preview deployment of this branch; see the PR for the build id and the run output (`docs/lab/retest-2026-09-24/verify-brief-flow.md`)._

## 5. Not verified (and why)

- **Physical phone**: only Chromium Android emulation was run; iPhone/WebKit is not installed in this container. Real keyboard behaviour on iOS is untested.
- **Voice**: no server transcription key is configured, so the Lab uses browser dictation; recording storage was verified structurally (tests) but not with a real provider.
- **Email delivery**: SES is still in the sandbox; brief-copy and magic-link emails to visitors are refused by AWS. Not verified.
- **Deletion via the emailed link**: depends on email delivery; the deletion code path (including stored files) is covered by structural tests only.
- **Screen-reader pass**: accessible names exist and contrast was measured; no assistive-technology session was run.
- **Privacy policy**: remains a draft pending counsel.
